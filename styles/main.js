import {StyleSheet} from "react-native";

const styles = StyleSheet.create({
    empty_view: {
        flex: 1,
        alignItems: 'center',
        margin: 30,
    },
    empty_small_text: {
        fontSize: 11,
    }
});

export {styles}